# Python dependencies for the validated benchmark target.
# Spark 3.5.9 is pinned because the DataFrame-integrated TorchDistributor path
# is version-sensitive; the exact cluster Java/Hadoop installation is captured
# in each retained run artifact.
torch>=2.2,<3
pyspark==3.5.9
numpy>=1.26,<3
pandas>=2.1,<3
scikit-learn>=1.4,<2
pyarrow>=14,<22
PyYAML>=6.0,<7
