# Python dependencies for the validated benchmark target.
# Spark 3.5.9 documents PyArrow <13 for its pandas-on-Spark/Arrow integration.
# PyArrow 12.0.1 is built against NumPy 1.x, so keep NumPy pinned to the tested ABI.
# The exact cluster Java/Hadoop installation is captured in each retained run artifact.
torch>=2.2,<3
pyspark==3.5.9
numpy==1.26.4
pandas>=2.1,<3
scikit-learn>=1.4,<2
pyarrow==12.0.1
PyYAML>=6.0,<7
